"use strict";

// Array
// Step 1, Declaration array type number
// let bilangan: Array<number> = [1,2,3,4,5];

// Step 2, Declaration array type number
let bilangan = [1, 2, 3, 4, 5];

// Tuples
let karyawan = ["Nabil", "Anugerah", 21];

let PersonSingle = {
    name: "Logi",
    age: 30,
    partner: null
};

// Void & Never
function log(message) {
    console.log(message);
}

function error(message) {
    throw new Error(message);
}
